ORG 000 / inicia programa no endereço 

Load L    //Load - carrega o valor da variável ou localização de memória chamada "L" em algum registrador da CPU. 
           Isso significa que o valor armazenado em "L" será colocado em um registrador para ser usado posteriormente.

Store NAME_L  //Store - o valor que foi carregado na etapa anterior (o valor de "L") será armazenado na localização de memória com o rótulo 
              "NAME_L". Isso significa que o valor de "L" será atribuído a uma variável chamada "NAME_L" na memória do computador.
Load e
Store NAME_e
Load t
Store NAME_t
Load i
Store NAME_i
Load c
Store NAME_c
Load i
Store NAME_i
Load a
Store NAME_a

Halt  /Termino da execução do programa, Quando o computador encontra essa instrução, 
       ele para imediatamente a execução do programa e não executa mais nenhuma instrução subsequente.

L, HEX 4C      Atribui o valor Hexadecimal 
e, HEX 65
t, HEX 74
i, HEX 69
c, HEX 63
i, HEX 69
a, HEX 61

/Reservar o espaço para o Nome Leticia 

NAME_L, HEX 0
NAME_e, HEX 0
NAME_t, HEX 0
NAME_i, HEX 0
NAME_c, HEX 0
NAME_i, HEX 0
NAME_a, HEX 0